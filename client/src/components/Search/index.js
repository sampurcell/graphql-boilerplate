/**
 * Import all your search components from this directory here.
 */
import OrganizationSearch from './OrganizationSearch';

/**
 * Export all your search components here.
 * When another component needs to be exported,
 * Export a new object and remove the default keyword,
 * So you can export multiple components and the importer can choose which ones to require.
 */
export default OrganizationSearch;
