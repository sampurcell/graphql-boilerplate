/**
 * Import all repository related components that will be used elsewhere first.
 */
import RepositoryList from './RepositoryList';
import REPOSITORY_FRAGMENT from './fragment';

/**
 * Then export those items with the RepositoryList as the default export.
 */
export { REPOSITORY_FRAGMENT };
export default RepositoryList;
