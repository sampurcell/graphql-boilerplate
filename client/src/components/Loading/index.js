import React from 'react';

/**
 * Simple FSC for the loading text.
 * It can be imported anywhere so we only have to write this once.
 */
const Loading = () =>
  <div className="loading-container">Loading...</div>

export default Loading;
